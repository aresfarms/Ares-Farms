{
  "displayName": "Furlong staging secret rotation request pending",
  "combiner": "OR",
  "enabled": true,
  "conditions": [
    {
      "displayName": "Rotation request unacknowledged for 15 minutes",
      "conditionThreshold": {
        "filter": "resource.type = \"pubsub_subscription\" AND resource.labels.subscription_id = \"furlong-secret-rotation-requests\" AND metric.type = \"pubsub.googleapis.com/subscription/oldest_unacked_message_age\"",
        "comparison": "COMPARISON_GT",
        "thresholdValue": 900,
        "duration": "0s",
        "trigger": {
          "count": 1
        },
        "aggregations": [
          {
            "alignmentPeriod": "60s",
            "perSeriesAligner": "ALIGN_MAX"
          }
        ]
      }
    }
  ],
  "notificationChannels": [
    "projects/furlong-staging-499102/notificationChannels/10163200903343322720"
  ],
  "documentation": {
    "mimeType": "text/markdown",
    "content": "A governed Secret Manager rotation reminder is waiting in the `furlong-secret-rotation-requests` subscription. Replace the credential at its source, add a new Secret Manager version, validate consumers, disable the superseded version after overlap, record non-secret evidence, then acknowledge the Pub/Sub message. A reminder alone is not rotation evidence."
  }
}
