import { PortableSurfaceIndexPage } from "@/app/portal/verticalSurfacePage";

export default function SponsorPortalPage() {
  return (
    <PortableSurfaceIndexPage
      audience="sponsor"
      title="Sponsor Portal"
      subtitle="Sponsor-facing coordination surfaces for readiness and advisory reporting without commitments, borrower disclosure, or production promotion."
    />
  );
}
